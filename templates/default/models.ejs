/** <%=data.summary %> <%=method %> <%=url %> */
export type <%= title %> = { <% if(definition.properties && Object.keys(definition.properties).length>0) {%><% for (let i = 0; i < Object.keys(definition.properties).length;
i++) { %> 
  /* <%- definition.properties[Object.keys(definition.properties)[i]].description %> */
  <%= Object.keys(definition.properties)[i] %><%=(definition.required && definition.required.length>0 && definition.required.includes(Object.keys(definition.properties)[i])) ? '' : '?' %>: <%- definition.properties[Object.keys(definition.properties)[i]].$ref ? definition.properties[Object.keys(definition.properties)[i]].$refTypeName : definition.properties[Object.keys(definition.properties)[i]].type === 'integer' ? 'number' : definition.properties[Object.keys(definition.properties)[i]].type === 'array' ? definition.properties[Object.keys(definition.properties)[i]].items['type'] + '[]' : definition.properties[Object.keys(definition.properties)[i]].type %>;  <% } %><% } %>
};
